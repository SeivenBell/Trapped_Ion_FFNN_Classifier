import re
import h5py

# filename = r"C:\Users\Seiven\Desktop\MY_MLmodels\ions2\binary\cropped_ions.h5"
# count = 0
# with h5py.File(filename, "r") as f:
#     for item in x: # Define a function to print with space separation
#     def print_with_space(x):
#         print(x, end=" ")
#         count = count + 1

#     # List all groups and datasets in the file
#     f.visit(print_with_space)
    


file_path = "C:/Users/Seiven/Desktop/MY_MLmodels/ions2/binary/cropped_ions.h5"
with h5py.File(file_path, 'r') as file:
    # You can print the keys to see the groups and datasets in the file
    
    # You can count the number of top-level items (groups and datasets) in the file
    number_of_items = len(file.keys())
    print("Number of items in the file:", number_of_items)

# file =  h5py.File(file_path, 'r')['bright_0_ion_0']
# print(file)